# P0-04 修复旗舰仓当前格式 CI - 执行记录（2026-08-28）

## 复现（先于修改）

- tiny-llm CI run https://github.com/open-infra-ai/tiny-llm/actions/runs/33042427022 (2026-08-27T05:25Z, FAILURE)：
  `./src/main.cpp:92:85: error: code should be clang-formatted [-Wclang-format-violations]`，clang-format-18 = 1:18.1.3-1ubuntu1
- paged-serving CI run https://github.com/open-infra-ai/paged-serving/actions/runs/33051255489 (2026-08-27T07:48Z, FAILURE)：
  `cargo fmt --all -- --check` 报 `tests/tokenizer_real_diff.rs:25` Diff

## 工具

- clang-format 18.1.8（uv tool install clang-format==18.1.8，与 CI 18.1.3 同 minor 系列；本机原无 clang-format）
- rustfmt 1.8.0-stable（rustc 1.90.0，本地 cargo fmt 输出与 CI 逐字节一致）

## 变更（最小格式修改）

### tiny-llm（src/main.cpp:92-93）
- 修改前：`<< " MHz"` 与 `<< std::endl;` 分两行（92 行 85 列溢出的违规点）
- 修改后：合并为单行（97 字符 < ColumnLimit 100）

### paged-serving（tests/tokenizer_real_diff.rs:25）
- `cargo fmt` 自动应用，仅此文件 1 处换行拆分（max_width=100 配置），与 CI 期望 diff 完全一致

## 验证（本地，全部实测）

```bash
# tiny-llm（CI 同款命令，clang-format-18 → clang-format）
find . -type f \( -name '*.cpp' -o -name '*.h' -o -name '*.cu' -o -name '*.cuh' \) \
  ! -path './build/*' ! -path './.git/*' ! -path './third_party/*' \
  ! -path './vendor/*' ! -path './external/*' ! -path './.kiro/*' \
  ! -path './tests/tokenizer_fixture_cases.h' | xargs clang-format --dry-run --Werror
# => exit 0（修复前 exit 123，仅 report main.cpp:92:85）

# paged-serving（CI 六步门禁，--locked）
cargo fmt --all -- --check          # exit 0
cargo clippy --all-targets -- -D warnings  # exit 0, Finished dev 24.11s
cargo build --locked                # exit 0, 27.41s
cargo test --locked                 # 153+4+0+4+15+38+0+0+1 = 215 passed, 0 failed; Doc-tests 17 passed; exit 0
cargo test --doc --locked           # 17 passed; exit 0
cargo doc --no-deps --locked        # exit 0
```

- 两仓 `git diff --check` 均通过
- 变更文件：
  - tiny-llm：src/main.cpp（格式）+ P0-03 名称替换文件（README/ROADMAP/docs/ffi.h/ffi.cpp/CHANGELOG Unreleased）
  - paged-serving：tests/tokenizer_real_diff.rs（格式）+ README.md（P0-03）
- 无无关重构；tiny-llm 未构建测试（属于 TLLM-01 范围）

## 限制与状态

- 本机 nvcc 12.0 / CI 11.8：clang-format/rustfmt 与编译环境无关，不影响本任务结论
- 未推送，未声称远端 CI 通过；本地门禁通过是 static/CPU 级（P0-04 范围本就只有格式门禁）
- 外部状态：local-only（0 提交）