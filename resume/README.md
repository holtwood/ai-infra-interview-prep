# 简历目录

- `resume-performance.zh.md`：推理性能/Kernel 方向，`tiny-llm` 为旗舰。
- `resume-serving.zh.md`：Serving 方向，`paged-infer` 先讲、`tiny-llm` 提供真实后端证据。
- 真实投递版请复制为 `resume-*.local.zh.md` 后填写手机号、微信、现居地等信息；
  `*.local.*` 已被 `.gitignore` 排除。

性能数字必须能追溯到技术仓的结果文件；测试数量和硬件快照更新时同步检查简历尾注。
公开简历只保留 GitHub 与可公开邮箱，不提交私人联系人或内推关系。
